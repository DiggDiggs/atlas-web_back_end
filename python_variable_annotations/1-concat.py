#!/usr/bin/env python3
""" The provided Python script starts with a shebang line indicating the use of Python 3 and is followed by a type-annotated function named concat, which takes two string parameters (str1 and str2) and returns a concatenated string, accompanied by appropriate function-level documentation describing its purpose. """


def concat(str1: str, str2: str) -> str:
    """ Concatenate two strings """
    return str1 + str2
